name: "Generate: GitHubEnterprise-3.8"
on:
  schedule:
    - cron:  '13 8 * * *'
jobs:
  generate-githubenterprise-3_8:
    runs-on: ubuntu-latest
    container:
      image: wyrihaximusnet/php:8.2-nts-alpine-slim-dev-root
    steps:
      - uses: actions/checkout@v3
      - uses: ramsey/composer-install@v2
      - uses: ramsey/composer-install@v2
        with:
          working-directory: "clients/GitHubEnterprise-3.8"
      - name: Download current spec file
        run: |
          rm etc/specs/GitHubEnterprise-3.8/previous.spec.yaml || true
          mv etc/specs/GitHubEnterprise-3.8/current.spec.yaml etc/specs/GitHubEnterprise-3.8/previous.spec.yaml
          curl -o etc/specs/GitHubEnterprise-3.8/current.spec.yaml https://raw.githubusercontent.com/github/rest-api-description/main/descriptions-next/ghes-3.8/ghes-3.8.yaml
      - name: Hash specs
        id: spec-hash
        run: |
          echo "current=php -r 'echo md5(file_get_contents("etc/specs/GitHubEnterprise-3.8/current.spec.yaml"));'" >> "$GITHUB_OUTPUT"
          echo "previous=php -r 'echo md5(file_get_contents("etc/specs/GitHubEnterprise-3.8/previous.spec.yaml"));'" >> "$GITHUB_OUTPUT"
      - name: Look current up spec version
        id: look-current-up-spec-version
        uses: mikefarah/yq@v4.33.3
        with:
          cmd: yq '.info.version' 'etc/specs/GitHubEnterprise-3.8/current.spec.yaml'
      - name: Look previous up spec version
        id: look-previous-up-spec-version
        uses: mikefarah/yq@v4.33.3
        with:
          cmd: yq '.info.version' 'etc/specs/GitHubEnterprise-3.8/previous.spec.yaml'
      - name: Generate spec diff
        id: spec-diff
        run: |
          curl -fsSL https://pb33f.io/openapi-changes/install.sh | sh 
          delimiter="$(openssl rand -hex 8)"
          echo "diff<<${delimiter}" >> "${GITHUB_OUTPUT}"
          echo "$(openapi-changes summary --no-color --no-logo etc/specs/GitHubEnterprise-3.8/current.spec.yaml etc/specs/GitHubEnterprise-3.8/previous.spec.yaml || true)" >> "${GITHUB_OUTPUT}"
          echo "${delimiter}" >> "${GITHUB_OUTPUT}"
      - name: Update Client to ${{ steps.look-current-up-spec-version.outputs.result }}
        run: |
          make generate-client GitHubEnterprise-3.8
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          branch: "GitHubEnterprise-3.8/from-${{ steps.look-current-up-spec-version.outputs.result }}-hash-${{ steps.spec-hash.outputs.current }}-from-${{ steps.look-previous-up-spec-version.outputs.result }}-hash-${{ steps.spec-hash.outputs.current }}"
          token: ${{ secrets.SUBSPLIT_GITHUB_TOKEN }}
          labels: |
            Automated PR
            New Version
            automerge
          title: "[GitHubEnterprise-3.8] Update to ${{ steps.look-current-up-spec-version.outputs.result }} (hash: ${{ steps.spec-hash.outputs.current }}) from ${{ steps.look-previous-up-spec-version.outputs.result }} (hash: ${{ steps.spec-hash.outputs.previous }})"
          body: |
            Detected Schema changes:
            ${{ steps.spec-diff.outputs.diff }}
          commit-message: |
            [GitHubEnterprise-3.8] Update to ${{ steps.look-current-up-spec-version.outputs.result }} (hash: ${{ steps.spec-hash.outputs.current }}) from ${{ steps.look-previous-up-spec-version.outputs.result }} (hash: ${{ steps.spec-hash.outputs.previous }})
            Detected Schema changes:
            ${{ steps.spec-diff.outputs.diff }}
